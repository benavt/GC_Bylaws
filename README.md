# GC Bylaws

Primary source: `GC_Bylaws.tex`

This repo uses the nested `union-docs-latex-class` repository for the shared
`union-doc.cls` class, fonts, and logos.

After cloning, initialize the nested repo with:

```bash
git submodule update --init --recursive
```
